
<%= simple_form_for(search_game_submission, remote: true) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <p>
    Trag deine e-Mail ein, wenn du am Gewinnspiel teilnehmen möchtest!
  </p>
  <hr>
  <div class="form-inputs">
    <%= f.input :first_name, placeholder: "Max, Julia..." %>
    <br>
    <%= f.input :email, placeholder: "max.muster@email.com" %>
    <br>
    <%= f.input :subscribe_to_newsletter, as: :boolean %>
    <br>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Am Wettbewerb teilnehmen", class: "btn btn-primary" %>
  </div>
<% end %>
